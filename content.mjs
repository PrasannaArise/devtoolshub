export const guides = {
json: {title:'Clean up an API response', input:'{"user":{"id":42,"active":true},"roles":["editor","viewer"]}', output:'{\n  "user": {\n    "id": 42,\n    "active": true\n  },\n  "roles": [\n    "editor",\n    "viewer"\n  ]\n}', action:'Format', tips:[['Why does my JSON fail validation?','Check for trailing commas, single quotes, or an unquoted key. For example, {"name":"Ada",} is invalid because of the final comma. JSON values must be strings, numbers, objects, arrays, true, false, or null.'],['Will formatting preserve every value?','Formatting parses and serializes the input. Duplicate keys keep their last value, and numbers use JavaScript precision. Put long account numbers and identifiers in quotes before processing. Use a text diff when exact preservation matters.']],related:['jwt','base64']},
jwt: {title:'Read a token without trusting it', input:'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiJkZW1vIiwiZXhwIjoxNzAwMDAwMDAwfQ.signature', output:'Header: {"alg":"HS256","typ":"JWT"}\nPayload: {"sub":"demo","exp":1700000000}\nExpires: 2023-11-14T22:13:20.000Z\nSignature: NOT verified', action:'Decode', tips:[['Why can I read a JWT without a secret?','A signed JWT usually encodes its header and payload as Base64URL. Encoding makes the data transportable; it does not hide it. The signature helps a verifier detect tampering, but this decoder does not check that signature. The example token uses a dummy signature.'],['Does “Not expired” mean a token is valid?','No. A server also needs to verify the signature, accepted algorithm, issuer, audience, and other applicable claims. The expiration result here only compares a numeric exp claim with your device clock. JWT timestamps are seconds, not milliseconds.']],related:['timestamp','base64']},
base64: {title:'Encode a short UTF-8 message', input:'Hello', output:'SGVsbG8=',action:'Encode',tips:[['Why are there equals signs at the end?','Base64 groups bytes into blocks. The = character pads a final incomplete block. This decoder accepts valid padded or unpadded text and ignores whitespace. It rejects characters outside standard Base64.'],['Can I decode a JWT segment here?','JWTs use Base64URL, which substitutes - and _ for + and /. Use the JWT Decoder for token segments. This Base64 tool expects UTF-8 text; decoded binary data may not be valid text and will produce an error.']],related:['jwt','url']},
uuid: {title:'Create identifiers for test records',input:'3',output:'Three UUID v4 values, one per line.\nExample shape: xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx\nHere y is 8, 9, a, or b; x is a hexadecimal digit.',action:'Generate',tips:[['Why is the result different each time?','Version 4 UUIDs use random values. This tool calls the browser’s secure randomUUID function once for each requested identifier. Generate a batch, copy the output, and keep those values if you need stable fixture IDs.'],['Can I use a UUID as a password or login token?','A UUID is an identifier. Do not treat a resource as protected just because its ID is hard to guess. Authentication, authorization, token expiry, and revocation need their own design. For database uniqueness, keep an appropriate unique constraint.']],related:['hash','json']},
timestamp: {title:'Recognize seconds versus milliseconds',input:'1700000000',output:'UTC: 2023-11-14T22:13:20.000Z\nSeconds: 1700000000\nMilliseconds: 1700000000000',action:'Seconds to date',tips:[['Why does my date appear in 1970?','You may have interpreted seconds as milliseconds. A modern Unix timestamp in seconds often has ten digits; milliseconds often has thirteen. That is a useful clue, not a universal validation rule. Choose the unit from the system that produced the value.'],['How do I avoid time-zone ambiguity?','For Date to timestamp, include Z for UTC or an explicit offset, such as 2023-11-15T03:43:20+05:30. These represent the same instant as the example. The local result depends on your device’s time zone.']],related:['jwt','cron']},
url: {title:'Encode a query parameter value',input:'coffee & tea',output:'coffee%20%26%20tea',action:'Encode',tips:[['Should I encode the whole URL?','Use this tool for one component, such as the value after q= in a search URL. Encoding a complete URL also encodes its : and / characters. When building URLs in code, URL and URLSearchParams can manage components for you.'],['Why does + remain + after decoding?','This tool decodes percent escapes with decodeURIComponent. It does not apply HTML form decoding, where + can stand for a space. A literal plus sign encodes as %2B. Invalid percent sequences produce an error.']],related:['base64','json']},
cron: {title:'Run a job every fifteen minutes',input:'*/15 * * * *',output:'Minute: 0, 15, 30, 45\nHour: every hour\nDay and month: every day\nTime zone: UTC\nNext runs depend on the current time.',action:'Next runs',tips:[['Which cron format does this tool support?','Use five fields in this order: minute, hour, day of month, month, day of week. For example, 0 9 * * 1-5 means 09:00 UTC Monday through Friday. This tool does not accept Quartz seconds fields or named months and weekdays.'],['Why can both day fields change the schedule?','With restricted day-of-month and day-of-week fields, standard cron allows either to match. For example, 0 9 1 * 1 runs on the first day of a month and on Mondays. If either day field begins with *, both field checks apply. Always compare with your scheduler’s own dialect and time-zone settings.']],related:['timestamp','regex']},
regex: {title:'Extract numbers from a log message',input:'Pattern: (\\d+)\nFlags: g\nText: job 42 finished in 150 ms',output:'Match 1: "42" at index 4; capture group "42"\nMatch 2: "150" at index 19; capture group "150"',action:'Test',tips:[['Why do I only get the first match?','Add the g flag to find repeated matches. Enter the expression itself, such as \\d+, without surrounding / characters. Use i for case-insensitive matching and m when ^ and $ should match line boundaries. Flags cannot be repeated.'],['Will this pattern work in Java or Python?','Not necessarily. This tester uses your browser’s JavaScript regular-expression engine. Escaping, Unicode behavior, and supported syntax can differ across languages and browser versions. Test in your target runtime before relying on the pattern. Expensive expressions are stopped after five seconds.']],related:['json','url']},
sql: {title:'Separate the main clauses of a query',input:'select id, name from users where active = true order by name;',output:'SELECT id,\n  name\nFROM users\nWHERE active = true\nORDER BY name;',action:'Format',tips:[['Does formatting check whether the query is correct?','No. This tool changes layout around common SQL keywords, but it does not parse a database schema, validate syntax, or run the query. Test the query in your database’s normal development environment.'],['When should I use a database-specific formatter?','Use one for procedural SQL, vendor-specific quoting, dollar-quoted strings, or complex nested queries. This basic formatter preserves standard quoted strings, identifiers, and comments, but does not cover every dialect. Keep your original query and review changes.']],related:['json','uuid']},
hash: {title:'Check a known SHA-256 digest',input:'abc',output:'ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad',action:'SHA-256',tips:[['Why do two similar texts produce different hashes?','The input is converted into UTF-8 bytes. A trailing newline, an extra space, or a visually identical character with different Unicode encoding changes those bytes and therefore the digest. Compare exact input bytes when reproducing a hash.'],['Can a hash protect a password or prove who sent a message?','Raw SHA is not a password-storage scheme. A plain digest also does not authenticate a sender: someone who changes the message can calculate a new digest. Password storage and message authentication require purpose-built approaches. This tool calculates unkeyed text digests only.']],related:['base64','uuid']}
};
export function renderGuide(id,tools,esc){const g=guides[id];if(!g)return '';return '<section class="tool-guide"><h2>'+esc(g.title)+'</h2><p>Choose <strong>'+esc(g.action)+'</strong> to try this example'+(id==='regex'?' using the pattern, flags, and text shown below':'')+'.</p><div class="example-grid"><div><h3>Input</h3><pre>'+esc(g.input)+'</pre></div><div><h3>Expected result</h3><pre>'+esc(g.output)+'</pre></div></div><h2>Common questions and mistakes</h2>'+g.tips.map(([q,a])=>'<h3>'+esc(q)+'</h3><p>'+esc(a)+'</p>').join('')+'<h2>Related tools</h2><ul>'+g.related.map(id=>{const t=tools.find(t=>t.id===id);return '<li><a href="/'+id+'">'+esc(t.name)+'</a> — '+esc(t.description)+'</li>';}).join('')+'</ul></section>';}
